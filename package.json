{
  "scripts": {
    "test:functional": "jest --testPathPattern=functional-tests --config=functional-tests/jest.config.js",
    "test:fe": "jest --testPathPattern=client/src"
  },
  "devDependencies": {
    "@shelf/jest-mongodb": "^2.2.2",
    "dotenv": "^16.0.0",
    "jest": "^28.0.1",
    "jest-puppeteer": "^6.1.0",
    "mongodb": "^4.5.0",
    "mongoose": "^6.3.1"
  }
}
